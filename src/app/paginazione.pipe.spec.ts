import { PaginazionePipe } from './paginazione.pipe';

describe('PaginazionePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginazionePipe();
    expect(pipe).toBeTruthy();
  });
});
